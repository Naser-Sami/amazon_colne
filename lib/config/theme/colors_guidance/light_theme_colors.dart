import 'package:flutter/material.dart';

class LightThemeColors {
  LightThemeColors._();

  static const primary = Color(0xFFFF9900); // Amazon Orange
  static const onPrimary = Color(0xFFFFFFFF); // White on Primary
  static const primaryContainer = Color(0xFFFFE0B2); // Light orange container
  static const onPrimaryContainer = Color(0xFF4E2A00); // Dark orange text
  static const primaryFixed = Color(0xFFFFE5CC); // Fixed lighter orange
  static const onPrimaryFixed = Color(0xFF4E2A00); // Darker orange text
  static const primaryFixedDim = Color(0xFFFFB85C); // Dimmed orange
  static const onPrimaryFixedVariant = Color(0xFF664300); // Variant orange text

  static const secondary = Color(0xFF232F3E); // Amazon Black (dark grayish blue)
  static const onSecondary = Color(0xFFFFFFFF); // White on Secondary
  static const secondaryContainer = Color(0xFFBCC6D4); // Light gray-blue container
  static const onSecondaryContainer = Color(0xFF1A242F); // Dark gray text
  static const secondaryFixed = Color(0xFFD1DAE5); // Light gray
  static const onSecondaryFixed = Color(0xFF1A242F); // Darker gray text
  static const secondaryFixedDim = Color(0xFF8E9BAA); // Dimmed gray-blue
  static const onSecondaryFixedVariant = Color(0xFF37475A); // Variant gray text

  static const tertiary = Color(0xFF3C4A5F); // Muted gray-blue for accents
  static const onTertiary = Color(0xFFFFFFFF); // White text
  static const tertiaryContainer = Color(0xFFE5E8EC); // Light gray container
  static const onTertiaryContainer = Color(0xFF1A242F); // Dark gray text
  static const tertiaryFixed = Color(0xFFE5E8EC); // Light gray
  static const onTertiaryFixed = Color(0xFF3C4A5F); // Muted gray-blue text
  static const tertiaryFixedDim = Color(0xFFBCC6D4); // Dimmed light gray
  static const onTertiaryFixedVariant = Color(0xFF4A5A6D); // Variant gray-blue text

  static const error = Color(0xFFD32D20); // Red for errors
  static const onError = Color(0xFFFFFFFF); // White on Error
  static const errorContainer = Color(0xFFF9D6D5); // Light red container
  static const onErrorContainer = Color(0xFF8C1D18); // Dark red text

  static const outline = Color(0xFF8E9BAA); // Muted gray for outlines
  static const outlineVariant = Color(0xFFBCC6D4); // Lighter variant for outlines

  static const scaffoldBackground = Color(0xFFF6F9FC); // Amazon light background
  static const background = Color(0xFFFFFFFF); // White background
  static const onBackground = Color(0xFF232F3E); // Dark text on background

  static const surface = Color(0xFFFFFFFF); // White surface
  static const onSurface = Color(0xFF232F3E); // Dark text
  static const surfaceVariant = Color(0xFFF0F4F8); // Slightly darker surface
  static const onSurfaceVariant = Color(0xFF37475A); // Darker gray-blue text

  static const inverseSurface = Color(0xFF232F3E); // Amazon Black surface
  static const inverseOnSurface = Color(0xFFF6F9FC); // Light text on dark surface
  static const inversePrimary = Color(0xFFFFB85C); // Lighter orange

  static const shadow = Color(0xFF000000); // Black for shadows

  static const surfaceTint = Color(0xFFFF9900); // Amazon Orange
  static const scrim = Color(0xFF000000); // Black overlay

  static const surfaceContainerHightest = Color(0xFFF7FAFD); // Lightest container
  static const surfaceContainerHight = Color(0xFFF3F6F9); // Slightly darker
  static const surfaceContainer = Color(0xFFEFF2F6); // Even darker
  static const surfaceContainerLow = Color(0xFFE9ECF1); // Darker still
  static const surfaceContainerLowest = Color(0xFFFFFFFF); // White container

  static const surfaceDim = Color(0xFFE6E9ED); // Dimmed light surface
  static const surfaceBright = Color(0xFFFFFFFF); // Brightest white surface

  static const text = Color(0xFF232F3E); // Dark text
  static const red = Color(0xFFD32D20); // Same as error
  static const green = Color(0xFF007E3C); // Amazon green for success
}
